s3_id: <%= ENV['S3_ID'] %>
s3_secret: <%= ENV['S3_SECRET'] %>
s3_bucket: dylanvann.com

max_age:
# CSS uses cache busting, so it's safe to cache for the maximum possible time.
  "css/*": 31536000
  "*": 300

gzip: true
